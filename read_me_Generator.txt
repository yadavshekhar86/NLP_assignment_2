Do run the assign2_part1_2.py to see the n-gram models in action.

The Generator function can have arguments 'bigram', 'trigram', 'quadgram'

The function returns a paragraph of five sentences.

The function can be called as:

a = Generator('bigram')

b = Generator('trigram')

c = Generator('quadgram')

 